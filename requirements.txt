xgboost==2.1.1          # Specific version to avoid compatibility issues
flask==3.0.3            # For web serving (if applicable)
gunicorn==22.0.0        # Production WSGI server
pandas==2.2.1           # For data handling
numpy==1.26.4           # Numerical operations
 
python-dotenv==1.0.1    # For environment variables